# Automation MCP Server Configuration

# Add only the credentials you actually need.
# Uncomment the blocks that apply to your setup.

# ── Agent HTTP Server (agent-server.ts) ──────────────────────────────
# Required when running the live agent server (npm run start:agent)
# AGENT_API_KEY=            # Shared secret between agent and Open WebUI
# AGENT_PORT=3002           # HTTP port (default: 3002)

# ── LLM Configuration ────────────────────────────────────────────────
# Used by agent-server.ts and whatsapp-poller.ts
OPENAI_API_KEY=
OPENAI_BASE_URL=
# Default model used when request does not specify one
LLM_MODEL=gpt-4o-mini
# Optional allowlist for model picker (/v1/models) and request validation
# Comma-separated model IDs, example:
# LLM_MODELS=gpt-4o-mini,gpt-4.1-mini,gpt-4.1
# GitHub Models example:
#   OPENAI_API_KEY=ghp_yourGitHubToken
#   OPENAI_BASE_URL=https://models.inference.ai.azure.com
#   LLM_MODEL=gpt-4o-mini

# ── Brunas TMS ───────────────────────────────────────────────────────
# Used by brunas-server.ts (local MCP) — NOT needed for agent-server.ts
# (agent-server uses Brunas auth via user credentials at login time)
# BRUNAS_API_URL=https://example.com
# BRUNAS_API_USERNAME=your_login
# BRUNAS_API_PASSWORD=your_password

# ── BSS Accounting ───────────────────────────────────────────────────
# BSS_API_URL=https://bss.example.com
# BSS_USERNAME=finance_user
# BSS_PASSWORD=super_secret

# ── WhatsApp bridge ──────────────────────────────────────────────────
# WHATSAPP_SESSION_PATH=./whatsapp-mcp/whatsapp-bridge/store

# ── Debug ────────────────────────────────────────────────────────────
DEBUG=false

# Brunas TMS Configuration
BRUNAS_EMAIL=
BRUNAS_PASSWORD=
BRUNAS_CLIENT_URL=https://demo.brunas.lt

# WhatsApp allowed users (only these numbers can use Brunas TMS via WhatsApp)
# Format: phone:Name, phone:Name
ALLOWED_WHATSAPP_USERS=

# WhatsApp poller config
POLL_INTERVAL_MS=5000
WHATSAPP_API_URL=http://localhost:8080
